import React from "react";
import styled from "styled-components";

export const Skills = () => {
    return (
        <SkillsStyle>

        </SkillsStyle>
    );
};

const SkillsStyle = styled.section`
  
`
